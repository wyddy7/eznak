Для работы с Cursor (особенно в режиме Composer Agent / Auto) главный секрет — **не просить все сразу**. ИИ начнет путаться в файлах и ломать зависимости. Нужно вести его за ручку по слоям программы: от базы к бэкенду, от бэкенда к боту, от бота к скедулеру.

**Как использовать:**
1. Создай файл `docs/tz/tz.md` и вставь туда финальное ТЗ из предыдущего сообщения.
2. Открой окно Cursor Composer (Ctrl+I / Cmd+I, убедись что стоит режим **Agent** или **Auto**).
3. Прикрепи файл ТЗ (напиши `@tz.md` в поле ввода Composer).
4. Копируй и отправляй промпты по очереди. Жди полного завершения каждого шага (и проверяй, что код запускается).

**Стек управления зависимостями:** uv (не pip). Команды: `uv sync` — установка, `uv run <cmd>` — запуск в venv, `uv add <pkg>` — добавление зависимости.

---

## 🚀 Череда Промптов для Cursor Agent

### Промпт 1: Инфраструктура и БД

```text
@tz.md
Начинаем реализацию проекта по ТЗ. Выступай в роли опытного Senior Python разработчика. Делай все шаги строго по ТЗ. Без оверинжиниринга.

Шаг 1: Инфраструктура и БД
1. Создай структуру папок project_root, backend/db, backend/core, bot/.
2. Создай `pyproject.toml` с зависимостями (или используй `uv add`). ОБЯЗАТЕЛЬНО включи: fastapi, uvicorn, sqlalchemy[asyncio], asyncpg, alembic, pydantic-ai, aiogram, structlog, apscheduler, pillow, aiohttp (версии не фиксируй). Создай `.env.example` с переменными по ТЗ.
3. Настрой `backend/db/models.py`: асинхронные модели SQLAlchemy для "channels" и "posts". Статусы постов: `draft`, `scheduled`, `posted`, `failed` (без `approved`).
4. Настрой `backend/main.py` (чистый FastAPI).
5. Инициализируй Alembic и настрой `env.py` (укажи target_metadata).

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам в терминале):
1. Установи зависимости (`uv sync`).
2. Сгенерируй первую миграцию: `uv run alembic revision --autogenerate -m "init"`.
3. Примени миграцию: `uv run alembic upgrade head`.
4. Запусти сервер: `uv run uvicorn backend.main:app --reload` и сделай curl-запрос (или проверь доступность) `http://127.0.0.1:8000/docs`. Затем ОБЯЗАТЕЛЬНО убей процесс сервера.

✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):
- В БД создались таблицы.
- Сервер FastAPI корректно запустился через uvicorn без ошибок.
- Напиши мне: "Шаг 1 завершен. Таблицы созданы, FastAPI работает через uvicorn." и ЖДИ мою команду.
---

### Промпт 2: Ядро - LLM Пайплайн

```text
@tz.md
Продолжаем.
Шаг 2: Реализация LLM-пайплайна.
1. Создай файл `backend/services/llm_pipeline.py`.
2. Напиши логику генерации с использованием `pydantic-ai`. Настрой провайдера OpenRouter прочитав перед этим документацию в context7 на первое и второе
3. Цепочка: Prompt 1 (генерация 15 фраз) -> Prompt 2 (ранжирование и возврат Pydantic-модели с лучшими фразами).
4. Добавь логирование (structlog).
```

**🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам):**
1. Создай временный файл `test_llm.py` в корне.
2. Напиши в нем вызов твоего пайплайна с тестовым `channel_id` и мок-датасетом.
3. Запусти `uv run python test_llm.py` в терминале.

**✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):**
- Скрипт `test_llm.py` отрабатывает без ошибок.
- В консоль выводится валидный JSON/объект со структурой RankedPhrase.
- Напиши мне: "Шаг 2 завершен. LLM стабильно возвращает данные. Готов к Шагу 3."

**Действие:** Убедись, что тестовый скрипт реально скачал фразы. Закоммить код.

---

### Промпт 3: Backend API Роуты

```text
@tz.md
Продолжаем.
Шаг 3: API Бэкенда и безопасность.
1. В `backend/api/` создай роутеры (generation.py, posts.py) и подключи их в `main.py`.
2. Создай Dependency `get_api_key`, который проверяет заголовок "X-API-Key" на совпадение с переменной `BACKEND_API_KEY` из .env. Защити все роуты этой зависимостью.
3. `POST /api/v1/generate` — вызывает llm_pipeline (для MVP channel_id — первый из БД).
4. `POST /api/v1/posts/approve` — принимает `{"phrases": [...]}`, создаёт записи в БД со статусом `draft`, возвращает `draft_posts` с `id`, `text`, `channel_id`, `suggested_time`.
5. `POST /api/v1/posts/schedule-batch` — принимает массив `[{"post_id", "channel_id", "time"}, ...]`, обновляет посты: `scheduled_at` и статус `scheduled`. Время в MSK (Europe/Moscow).
```

**🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам):**
1. Запусти `uv run uvicorn backend.main:app`.
2. Сделай 2 запроса через `curl` или встроенный python script: без токена (ждём 401) и с токеном (ждём 200).
3. Убей процесс сервера.

**✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):**
- Эндпоинты защищены.
- Напиши мне: "Шаг 3 завершен. Роуты работают и защищены. Готов к Шагу 4."

**Действие:** Закоммить код.

---

### Промпт 4: Каркас Бота и Аутентификация

```text
@tz.md
Шаг 4: Telegram Bot (Интерфейс и база).
1. Настрой `bot/bot.py` (aiogram).
2. Напиши middleware (или проверку), которая читает `ADMIN_TELEGRAM_IDS` из среды и игнорирует апдейты от других пользователей.
3. Создай Reply-меню: "🎲 Сгенерировать", "📅 Ожидают постинга".
4. Логика генерации: при нажатии "Сгенерировать", бот отправляет `POST /api/v1/generate` (с заголовком X-API-Key, channel_id — первый из БД) и выводит фразы с Inline-кнопками [✅ Approve] [❌ Reject] [✏️ Edit].
```

**🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:**
Скажи мне запустить бота: `uv run python -m bot.bot` и FastAPI.
Я сам зайду в Telegram, нажму "Сгенерировать" и проверю, появляются ли кнопки.

**✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):**
- Код бота написан.
- Напиши мне: "Код бота готов. Запусти сервер и бота, потыкай кнопки в ТГ. Если всё ок, дай команду на следующий шаг."

**Действие:** Потыкай бота в ТГ. Если бот шлет фразы — коммить.

---

### Промпт 4.5: Логирование и отладка (structlog + stdlib)

```text
@tz.md
Промежуточный шаг. Шаг 4.5: Интеграция structlog с stdlib logging.
1. Прочитай документацию structlog (Standard Library) в context7. Настрой structlog так, чтобы он использовал `structlog.stdlib.LoggerFactory()` и `logging` как backend.
2. В `backend/core/logging.py` настрой единый pipeline: structlog → stdlib → один StreamHandler (stdout). Используй `ProcessorFormatter` или `structlog.stdlib.recreate_defaults()` по документации.
3. Убедись, что логи приложения (api.generate.*, llm_pipeline_*) и логи uvicorn выводятся в один поток и видны в консоли при запросах.
4. Создай скрипт `scripts/seed_channel.py`: асинхронно подключается к БД, вставляет один канал в таблицу `channels` (telegram_id, name — любые тестовые), если таблица пуста. Запуск: `uv run python scripts/seed_channel.py`.
```

**🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам):**
1. Запусти `uv run python scripts/seed_channel.py` — канал должен появиться в БД.
2. Запусти `uv run uvicorn backend.main:app --reload` и бота. Нажми «Сгенерировать» в ТГ.
3. В консоли uvicorn должны появиться логи: `request`, `api.generate.channel_found`, `llm_pipeline_start`, `api.generate.success` (или `api.generate.no_channel` / `api.generate.empty_phrases` при ошибке).

**✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):**
- Логи видны в консоли при каждом запросе.
- После seed — фразы генерируются и отображаются в боте.
- Напиши мне: "Шаг 4.5 завершен. Логирование работает, seed выполнен, генерация фраз ок."

**Действие:** Закоммить. Переходим к FSM.

---

### Промпт 5: Модерация и FSM Бота

```text
@tz.md
Шаг 5: Планирование в Боте (Approve/Reject/Edit уже реализованы в Шаге 4).
1. Approve/Reject/Edit уже делают: обновляют текст сообщения со статусом (✅ Approved, ❌ Rejected, ✏️ Отредактировано). Reject НЕ удаляет сообщение — сохраняет историю в чате.
2. Добавь кнопку «Отправить на планирование» или логику: при Approve бот собирает одобренные фразы и шлёт в `POST /api/v1/posts/approve`. Получает `draft_posts` с слотами.
3. Показывай слоты. Shuffle — тасуй в памяти. Confirm All — `POST /api/v1/posts/schedule-batch`.
4. Время по МСК.
```

**🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:**
Напиши мне, что код обновлен. Я снова запущу бота, нажму Edit, введу новый текст и назначу время. Если в консоли (и БД) появится запись — мы прошли.

**✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):**
Жди моей обратной связи.

**Действие:** Проверить стейты FSM. Закоммить.

---

### Промпт 6: Scheduler (Планировщик постов)
@tz.md
Переходим к Шагу 6: APScheduler и автопостинг в Telegram.
Нам нужно научить бэкенд публиковать одобренные посты по расписанию. В будущем целевой канал будет браться из БД, но для текущего MVP мы захардкодим его в переменные окружения.

ЗАДАЧИ:
1. Конфигурация:
   - В `backend/core/config.py` (или где лежат настройки) добавь переменную `TARGET_CHANNEL_ID` (тип str).
   - Бэкенд должен читать её из `.env`.

2. Настройка планировщика:
   - В `backend/services/scheduler.py` настрой `AsyncIOScheduler` (timezone="Europe/Moscow").
   - Создай job, который запускается раз в 1 минуту.
   - Логика: ищем в БД посты, где `status == 'scheduled'` и `scheduled_at <= now() (в МСК)`.

3. Отправка поста:
   - Отправляй текст поста в канал `TARGET_CHANNEL_ID` через aiohttp прямым POST-запросом к Telegram API (`https://api.telegram.org/bot<BOT_TOKEN>/sendMessage`). 
   - В случае успешной отправки (HTTP 200) — меняй статус поста в БД на `posted` и пиши красивый лог `structlog` (с ID поста и текстом).
   - В случае ошибки — лови exception, пиши лог `error` и НЕ меняй статус (чтобы попробовать снова).

4. Интеграция:
   - Запускай и останавливай Scheduler в `lifespan` FastAPI (`backend/main.py`).

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Тестовый прогон):
1. Измени один из существующих постов в БД: поставь ему `status = 'scheduled'` и время `scheduled_at = now() + 1 минута`.
2. Запусти FastAPI. Жди минуту.

✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):
- Пост улетел в целевой канал.
- В консоли появился лог "post_published".
- В БД статус изменился на `posted`.

Сообщи мне, когда закончишь писать код. Я сам пропишу канал в .env и протестирую флоу.
---

### Промпт 7: Генерация картинок (Медиа - Финал MVP)

```text
@tz.md
Финальный мазок. Шаг 7: Генератор медиа.
1. В `backend/services/media_gen.py` напиши функцию генерации картинки через Pillow (берет сток из assets/templates/, центрирует текст поста).
2. В скедулере перед постингом добавь вероятность: $P \le 0.3$.
3. Если сработало: отправляем байты как photo (sendPhoto). Иначе - обычный sendMessage.
```

**🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам):**
1. Скачай/сгенерируй тестовый фон и положи в `assets/templates/bg.jpg`.
2. Напиши `test_media.py`, передай текст. Скрипт должен сохранить файл "output.jpg". Убедись, что текст не вылезает за края.

**✅ КРИТЕРИИ УСПЕХА (Остановись, когда выполнишь):**
- Скрипт генерирует изображение.
- Напиши: "MVP полностью готов! Картинки сгенерированы. Проект завершен."

**📋 Чек-лист проверки Шага 7 (выполни сам):**

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | `uv run python scripts/create_bg.py` (если нет bg.jpg) | Создан `assets/templates/bg.jpg` |
| 2 | `uv run python test_media.py` | Появился `output.jpg`, текст по центру, не вылезает |
| 3 | Запустить FastAPI + бота | Сервисы стартуют без ошибок |
| 4 | Запланировать пост на `now()+1 мин`, дождаться | Пост в канале (текст или картинка с вероятностью ~30%) |
| 5 | Проверить логи | `post_published` с `has_media=true/false` |

**✅ Тудушки Шага 7:**
- [ ] Создать/скачать `assets/templates/bg.jpg` (или запустить `scripts/create_bg.py`)
- [ ] Запустить `uv run python test_media.py` — проверить output.jpg
- [ ] Запустить FastAPI, запланировать пост, проверить постинг с/без картинки


----- эпик 2. Приведение MVP в мультитенант и учтение техдолгов

Прежде чем мы напишем промпты для Cursor, давай **расставим приоритеты**. Главное правило инженерии: *сначала меняем фундамент (БД и конфиги), потом бизнес-логику (API), и только в конце — интерфейс (UI) и рюшечки (картинки).*

### 📊 Приоритеты (от фундамента к UI):
1. **Конфиги (Рефакторинг):** Выносим хардкод (промпты, вероятности, настройки картинок) в удобные файлы.
2. **БД и Мультитенантность (Фундамент):** Учим базу данных понимать разные каналы и хранить датасеты (референсы) для каждого из них.
3. **LLM и Вариативность:** Учим пайплайн брать случайные фразы из БД для промпта (побеждаем детерминированность нейросетей).
4. **API и Post Now:** Добавляем эндпоинты для немедленного постинга и обновляем старые под мультитенантность.
5. **Bot UI (Выбор канала и время):** Учим бота спрашивать «в какой канал постим?» и добавляем FSM для ручного ввода времени.
6. **Медиа (Координаты):** Добавляем случайные позиции текста на картинках.

*(Примечание: парсинг чужого ТГК — отдельный скрипт на `Telethon`/`Pyrogram`. Мы делаем таблицу и логику — заполняешь руками или скриптом.)*

**Промпты:** Сейчас все каналы используют одни промпты из YAML. Разные промпты для разных каналов — в техдолг (таблица `channel_prompts` в БД).

---

### 🚀 Череда Промптов для Cursor Agent (v2.0)

Инструкция: Скопируй новые требования в свой `tz.md` (или создай `tz_v2.md`). Открывай Cursor Composer (Agent) и скармливай промпты по одному, строго дожидаясь успешной проверки каждого.

#### Промпт 1: Вынос конфигов в YAML
```text
@tz.md
Мы начинаем итерацию v2.0 поверх стабильного MVP. Выступай как Senior Python Backend Developer. Идем строгими шагами.

Шаг 1: Конфиги в YAML (удобно править руками).
1. Добавь зависимость: `uv add pyyaml`.
2. Создай папку `config/` в корне проекта.
3. Создай `config/prompts.yaml`: промпты Генератора и Критика (system + user_template). Перенеси туда тексты из `backend/core/prompts.py`.
4. Создай `config/media.yaml`: `image_probability: 0.3`, список позиций текста в формате `[{x: 0.5, y: 0.5}, {x: 0.5, y: 0.15}, ...]` (доли 0–1: center=0.5, top≈0.15, bottom≈0.85).
5. В `backend/core/config.py` добавь загрузку YAML (или отдельный модуль `config_loader.py`). Промпты и настройки медиа читаются при старте.
6. Измени `llm_pipeline.py` и `media_gen.py` — импорт из конфига вместо хардкода. Удали/замени `prompts.py` на чтение из YAML.

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам):
Запусти `test_llm.py` и `test_media.py`. Убедись, что генерация и картинки работают.

✅ КРИТЕРИИ УСПЕХА:
- config/prompts.yaml и config/media.yaml существуют и читаются.
- test_llm.py отрабатывает без ошибок.
- test_media.py генерирует output.jpg.
- llm_pipeline и media_gen берут данные из конфига, не из хардкода.
Напиши: \"Шаг 1 завершен. Конфиги в YAML, код работает.\" и ЖДИ.
```

#### Промпт 2: База Данных (Мультитенантность и Датасеты)
```text
@tz.md
Шаг 2: Расширение БД для мультитенантности и датасетов.
1. В `backend/db/models.py` таблица `channels` уже есть (telegram_id, name). Убедись, что поля на месте.
2. Добавь таблицу `datasets`: id (UUID), channel_id (FK), text (TEXT) — одна эталонная фраза на строку.
3. Добавь эндпоинт `POST /api/v1/channels/{channel_id}/datasets/bulk` или скрипт для bulk insert фраз в датасет канала.
4. Сгенерируй миграцию Alembic и примени её.
5. Обнови `scripts/seed_channel.py` (или создай `scripts/seed.py`): добавляет два канала (telegram_id: -1003812297034 и -1003592368865) + по 5 постов на каждый с `scheduled_at` в ближайшие минуты (чтобы сразу проверить постинг).
6. Прочитай `references/eto znak/messages4.html` — это экспорт канала «ЭТО ЗНАК». Извлеки из HTML текстовые посты (ищи `div.text` внутри `div.message.default`). Выбери 5 мотивационных фраз в стиле канала и напиши код/запрос для вставки их в таблицу `datasets` для одного из созданных каналов. Парсинг и insert — делай сам.

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ (Выполни сам):
1. `uv run alembic revision --autogenerate -m "add datasets"`
2. `uv run alembic upgrade head`
3. `uv run python scripts/seed_channel.py` (или seed.py)

✅ КРИТЕРИИ УСПЕХА:
- Таблица datasets создана (миграция применена).
- В БД 2 канала с telegram_id -1003812297034 и -1003592368865.
- По 5 постов на канал с scheduled_at в ближайшие минуты.
- 5 фраз в датасете для канала (из messages4.html).
- Эндпоинт bulk или скрипт для insert работает.
Напиши: \"Шаг 2 завершен. Таблица datasets создана, сидирование прошло.\" и ЖДИ.
```

#### Промпт 3: Умный LLM-пайплайн (Рандомизация)
```text
@tz.md
Шаг 3: Вариативность промптов (борьба с детерминированностью).
1. В `backend/services/llm_pipeline.py` функция `run_llm_pipeline` уже принимает `channel_id`. Добавь запрос в БД: SELECT фразы из `datasets` WHERE channel_id = ?.
2. Если датасет пустой — fallback на дефолтный список из YAML (config/prompts.yaml, секция default_dataset) или из старого prompts.py.
3. Сделай случайную выборку: `random.sample(dataset, min(20, len(dataset)))` — 15–20 фраз.
4. Подставляй эту выборку в user_template промпта (промпты читай из YAML).

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:
Создай `test_random_llm.py`: вызови пайплайн для канала из seed. Запусти скрипт.

✅ КРИТЕРИИ УСПЕХА:
- test_random_llm.py запускается без ошибок.
- В логе видно, что фразы пришли из datasets (не из DEFAULT_DATASET).
- Выборка случайная (разные запуски — разные фразы).
- LLM возвращает валидный список RankedPhrase.
Напиши: \"Шаг 3 завершен. Вариативность внедрена.\"
```

#### Промпт 4: Обновление API (Включая Post Now)
```text
@tz.md
Шаг 4: Мультитенантность в API и немедленный постинг.
1. `POST /api/v1/generate`: body строго `{"channel_id": "uuid"}`. Убери логику «первый канал из БД».
2. `GET /api/v1/channels`: возвращает список каналов (id, name, telegram_id) — для выбора в боте.
3. `POST /api/v1/posts/approve`: body `{"channel_id": "uuid", "phrases": [...]}` — создаёт посты для указанного канала.
4. `POST /api/v1/posts/post-now`: body `{"post_id": "uuid"}`. Находит пост, достаёт `post.channel.telegram_id`, отправляет туда (sendMessage или sendPhoto с вероятностью из config/media.yaml, 0.3). Меняет статус на `posted`.
5. В `scheduler.py`: исправь баг — вероятность картинки должна быть 0.3 (не 0.03!). Целевой канал — `post.channel.telegram_id`, а не TARGET_CHANNEL_ID из .env.
6. Ответы API: везде включай `channel_name` где релевантно.

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:
curl к /generate с channel_id, curl к /channels, curl к post-now (404 ок, если поста нет).

✅ КРИТЕРИИ УСПЕХА:
- POST /generate с body {\"channel_id\": \"uuid\"} — 200, без channel_id — 422.
- GET /channels — список каналов с id, name, telegram_id.
- POST /posts/post-now с post_id — пост уходит в канал поста, статус posted.
- Scheduler использует post.channel.telegram_id и вероятность 0.3.
- В ответах API есть channel_name.
Напиши: \"Шаг 4 завершен. API мультитенантен, post-now работает, scheduler шлёт в канал поста.\"
```

#### Промпт 5: UI Бота Часть 1 (Выбор канала и Post Now)
```text
@tz.md
Шаг 5: Интерфейс бота (Каналы и Кнопки).
1. При нажатии \"🎲 Сгенерировать\": бот вызывает `GET /api/v1/channels`, показывает Inline-кнопки с названиями каналов. После выбора канала — `POST /generate` с `channel_id`. Храни `channel_id` в FSM на весь флоу.
2. Везде в тексте (одобрение, расписание) выводи название выбранного канала.
3. `_post_approve` передаёт `channel_id` в body. `_post_generate` — `channel_id`.
4. В списке draft_posts под каждым постом добавь кнопку `[🚀 Post Now]` (callback_data: `post_now:{post_id}`).
5. Обработчик Post Now: `POST /api/v1/posts/post-now` с `post_id`. Обновить сообщение на \"✅ Опубликовано прямо сейчас\".

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:
Запусти бота: сгенерировать → выбрать канал → Post Now.

✅ КРИТЕРИИ УСПЕХА:
- При \"Сгенерировать\" показываются Inline-кнопки с каналами.
- После выбора канала идёт генерация, channel_id хранится в FSM.
- В тексте флоу отображается название канала.
- У каждого draft_post есть кнопка [🚀 Post Now].
- Post Now публикует пост и обновляет сообщение на \"✅ Опубликовано прямо сейчас\".
Напиши: \"Код бота обновлен. Тестируй Post Now и выбор канала.\" ЖДИ обратной связи.
```

#### Промпт 6: UI Бота Часть 2 (Ручное время FSM)
```text
@tz.md
Шаг 6: Ручной ввод времени (FSM).
1. В флоу расписания (слоты draft_posts) добавь под каждым постом кнопку `[⏰ Задать время вручную]` (callback_data: `manual_time:{post_id}` — post_id можно обрезать/хешировать, если UUID не влезает в 64 байта).
2. FSM: `ManualTimeStates.waiting_for_time`, в state_data — `post_id` (или индекс в draft_posts).
3. Бот просит: \"Введи время в формате ДД.ММ.ГГГГ ЧЧ:ММ (МСК)\".
4. Парсер: `dateutil.parser` или `datetime.strptime`. Таймзона MSK. При ошибке — \"Неверный формат, попробуй снова\".
5. Обязательно используй `zoneinfo.ZoneInfo(\"Europe/Moscow\")` (или `pytz`), чтобы получаемый `datetime` был timezone-aware (MSK). Если база данных использует UTC, сделай конвертацию MSK -> UTC перед сохранением.
6. При успехе: обновить `suggested_time` в draft_posts в state, обновить сообщение с расписанием.

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:
Пройди до планирования, нажми «Задать время вручную», введи 25.12.2026 15:30.

✅ КРИТЕРИИ УСПЕХА:
- Под каждым постом в слотах есть кнопка [⏰ Задать время вручную].
- Бот запрашивает время в формате ДД.ММ.ГГГГ ЧЧ:ММ.
- Ввод \"25.12.2026 15:30\" обновляет suggested_time и сообщение.
- Неверный формат — сообщение \"Неверный формат, попробуй снова\".
Напиши: \"FSM для времени написан. Тестируй ручной ввод.\"
```

#### Промпт 7: Умная генерация картинок
```text
@tz.md
Шаг 7: Случайное позиционирование текста на медиа.
1. В `backend/services/media_gen.py` функция `generate_post_image` должна читать позиции из `config/media.yaml` (список `{x: float, y: float}` — доли 0–1).
2. Если `position` не передан — выбирай случайную позицию из конфига: `random.choice(positions)`.
3. Pillow: координаты `(int(w * x), int(h * y))`, anchor="mm" для центрирования текста в точке.
4. Вызови `generate_post_image` 3 раза без явного position — сохрани как test1.jpg, test2.jpg, test3.jpg. Текст в разных местах.

🛑 ДЕЙСТВИЕ ДЛЯ ПРОВЕРКИ:
`uv run python -c "from backend.services.media_gen import generate_post_image; ..."` — 3 картинки с разными позициями.

✅ КРИТЕРИИ УСПЕХА:
- Позиции читаются из config/media.yaml.
- generate_post_image без position выбирает случайную позицию.
- test1.jpg, test2.jpg, test3.jpg — текст в разных местах (center, top, bottom и т.д.).
- Картинки генерируются без ошибок.
Напиши: \"Медиа генератор улучшен! Релиз v2.0 завершен! 🎉\"